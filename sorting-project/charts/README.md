# Charts Folder

This folder stores runtime comparison charts.

Expected chart files:

```text
worst_case_chart.png
average_case_chart.png
```

You can generate these charts by running:

```bash
cd scripts
python generate_charts.py
```
